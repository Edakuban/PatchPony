global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: patchpony-postgres-exporter
    static_configs:
      - targets: [postgres-exporter:9187]