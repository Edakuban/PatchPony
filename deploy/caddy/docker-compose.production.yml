services:
  caddy:
    ports: !reset
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/caddy/Caddyfile.production:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config