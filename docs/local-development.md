# Lokale Entwicklung

1. Kopiere `.env.example` nach `.env`.
2. Trage den destination.one-API-Endpunkt und den API-Schlüssel ausschließlich in `.env` ein.
3. Verwende für erste Tests das Modell `gpt-oss:20b`.
4. Lege ein lokales Entwicklungspasswort in `PATCHPONY_AUTH__DEVELOPMENTPASSWORD` fest.

Die Werte `PATCHPONY_AI__ENDPOINT`, `PATCHPONY_AI__APIKEY` und `PATCHPONY_AUTH__DEVELOPMENTPASSWORD` dürfen nicht geloggt werden.
